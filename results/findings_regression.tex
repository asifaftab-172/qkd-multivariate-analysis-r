Multivariate multiple regression jointly models QBER and log-SKR using attenuation centered at 21.69 dB, five noise-condition levels, and their interaction. Baseline is the reference condition. At the center, its fitted QBER is 0.0463 and fitted log-SKR is 3.905; its slopes per additional dB are +0.0015 and -0.170, respectively. Condition coefficients describe differences at the center, and interaction coefficients describe differences from the baseline slopes.

The within-sample $R^2$ values are 0.816 for QBER and 0.748 for log-SKR. For the attenuation-by-condition interaction, the sequential Pillai statistic is 0.168, with approximate $F=48.26$ and reference $p$ $<0.001$. The displayed MANOVA tests are sequential in formula order; with an interaction present, main-effect coefficients must be interpreted at the reference condition or centered attenuation.

The residual-versus-fitted and normal Q--Q plots assess curvature, unequal spread, and non-normal tails. Among observed rows, 0 fitted QBER values fall outside $[0,1]$ and 0 fitted log-SKR values fall below zero, indicating whether the unconstrained linear model crosses physical boundaries. Standard errors and reference tests assume an error structure that repeated within-setting measurements may violate. The regression is therefore a descriptive association model for this campaign, not evidence of causation or confirmed population-level significance.
